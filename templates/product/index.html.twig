{% extends 'mabase.html.twig' %}

{% block title %}Hello ProductController!
{% endblock %}

{% block body %}
	
	<div class="container">
    <h1>Liste des produits</h1>
		<table class="table table-striped">
			<thead>
				<tr>
					<th scope="col">ID</th>
					<th scope="col">Name</th>
                    <th scope="col">Details</th>
					<th scope="col">Supprimer</th>
				</tr>
			</thead>
			<tbody>


				{% for p in products %}
					<tr>
						<th scope="row">{{ p.id }}</th>
						<td>{{ p.name }}</td>
                        <td><a href="{{ path("app_product_show",{"id":p.id})}}">Detail</a></td>
					    <td><a href="{{ path("app_product_delete",{"id":p.id})}}">Supprimer</a></td>
					</tr>


				{% endfor %}
			</tbody>
		</table>
	</div>


{% endblock %}
